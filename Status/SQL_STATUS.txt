SQL Status Summary
Date: 2026-03-30
Status Folder: C:\Users\tanay\Desktop\Internship\Status
Backend Repo: C:\Users\tanay\Desktop\Internship\mentor-platform\mentor-platform

Current SQL/database work implemented
1. Database connection exists
- Oracle DB is configured in `application.properties`.
- JPA is enabled.
- Hibernate DDL is set to `update`.

2. Current entity/table coverage
- `User` entity exists.
- Expected current table: `users`
- Current stored columns are effectively:
  - `id`
  - `email`
  - `password`
  - `role`

3. Current repository coverage
- `UserRepository` exists.
- Current query support is only for users.

What is left in SQL part
1. Session data model is missing
- no `sessions` table
- no session owner/creator field
- no start time / end time
- no session status
- no session code persistence

2. Invitation model is missing
- no `session_invitations` table
- no invite sender / receiver
- no accepted / rejected / pending status
- no invite timestamps

3. Message persistence is missing
- no `messages` table
- no sender reference
- no session reference
- no created-at timestamp
- no message history retrieval

4. Editor persistence is missing
- no editor snapshots table
- no room-based saved code state
- no language persistence per session

5. Relationship design is missing
- no foreign-key mapping from sessions to users
- no foreign-key mapping from messages to sessions
- no foreign-key mapping from invites to sessions/users

6. Constraints and data quality are incomplete
- `email` should be unique at DB level if not already enforced
- role values should be constrained to valid values
- audit timestamps are not present
- no indexing strategy is defined for lookup-heavy fields

7. SQL migration discipline is missing
- no explicit migration tool setup like Flyway/Liquibase
- schema currently depends on Hibernate auto-update
- no versioned migration history

8. Reporting/history support is missing
- no session history table/view
- no mentor/student session tracking
- no stored analytics or usage summaries

Recommended next SQL order
1. Make `users.email` unique and clean up constraints
2. Add `sessions` table/entity
3. Add `messages` table/entity
4. Add `session_invitations` table/entity
5. Add timestamps and foreign keys everywhere needed
6. Introduce migration files instead of relying only on Hibernate update

Honest SQL maturity summary
- Database integration exists.
- Authentication user storage exists.
- Core product tables for sessions, messages, invitations, and history are still not implemented.
- SQL is currently the most incomplete major area after deployment.
